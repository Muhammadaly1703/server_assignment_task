# PostgreSQL Configuration
POSTGRE_USER=postgres         # PostgreSQL username (usually 'postgres' unless you've created a custom user)
POSTGRE_PASSWORD='Hussaini'   # PostgreSQL password (make sure to use your actual password, no spaces around it)
DB_NAME=employee_management   # Database name for your app
DB_HOST=localhost             # Database host (typically 'localhost' unless you're using a different host)

# JWT Secret (if you're using JWT authentication)
JWT_SECRET='your_secret_key'  # Secret key for JWT tokens (replace 'your_secret_key' with your actual JWT secret)
